#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  (pushnew (truename ".") ql:*local-project-directories*)
  (ql:register-local-projects)
  (ql:quickload :miscoined))

(defpackage :ros.script.run.3721212000
  (:use :cl))
(in-package :ros.script.run.3721212000)

(defun main (&rest argv)
  (miscoined:start :port (car argv))
  (loop (sleep 1000)))
;;; vim: set ft=lisp lisp:
